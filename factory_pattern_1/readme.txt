Use factory pattern when:
1. Not sure about the type of object we might have to create or work on

Pros:
1. Separates the object creation code from code which uses the product. So anytime
   we want to add a new product we can just extend it without changing anything else.