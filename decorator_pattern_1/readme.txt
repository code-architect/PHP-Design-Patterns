we use decorator pattern to assign a behaviour to an object dynamically without
modifying the code that uses the object (open close principle)

We use decorator pattern when using inheritance is not an option.

